
export function createMessage(available: boolean) {
  return available === true
    ? "Going on vacations"
    : "Too busy for vacations...";
}

// export const createMessage = (available: boolean) =>
//   available === true ? "Going on vacations" : "Too busy for vacations...";


